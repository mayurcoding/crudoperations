import { createSlice } from '@reduxjs/toolkit'

const initialState = { value: 0 }

const userSlice = createSlice({
  name: "user",
  initialState,
  reducers: {},
});

export const { } = userSliceSlice.actions;
export default userSlice.reducer